#include<stdio.h>
#include<stdlib.h>

int main() {
	printf("输入一个数\n");
		int m,n,i;
		int g,s,b;
	

scanf("%d%d",&m,&n);
	for(i=m;i<=n;i++) {
		g = i % 10;
		s = (i / 10) % 10;
		b = i / 100;
			if(i==g*g*g+s*s*s+b*b*b)
				{ 
					printf("%d\n",i);
			}
			else ;
	}
	return 0;
}

 
